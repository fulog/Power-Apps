# SharePointリストの情報を可視化・出力する Power Apps & Power Automate

SharePoint サイト内の **リスト／ライブラリ情報を一覧で可視化**し、  
さらに **Hidden の切り替え** や **列情報の Excel 貼り付け用テキスト出力** ができる  
Power Apps（キャンバスアプリ）＋ Power Automate（クラウドフロー）のセットです。

この記事で解説しているアプリのソース一式を公開しています。  
👉 ブログ記事: [SharePointリストの情報を可視化・出力するPower AppsとPower Automateの構成  ](https://www.fulogabc.net/entry/sharepoint-list-visualize-export-powerapps-powerautomate)

---

## 🚩 このアプリでできること

SharePoint リストを長く運用していると、次のようなことが気になってきます。

- どのリストが Hidden になっているか一目で把握したい
- 設計の見直しや棚卸しのために、列情報を Excel で一覧にしたい

このアプリを使うと、以下のことができます。

- 指定したサイト URL に対して、**リスト／ライブラリの一覧を取得してギャラリー表示**
- 一覧で確認できる情報
  - リスト／ライブラリ種別（BaseType）
  - 表示名（Title）
  - サーバー相対 URL（ServerRelativeUrl）
  - GUID（Id）
  - 説明文（Description）
  - 表示／非表示（Hidden）★ボタンで true/false 切り替え可能
- 任意のリストを選択し、その **列（フィールド）情報をギャラリー表示**
- 「Excelにコピー」ボタンで、列情報を
  - **タブ区切りテキスト**
  - クリップボードにコピー → そのまま Excel に貼り付け可能

これにより、以下のような一覧をすぐに作成できます。

- リスト設計書
- Power Apps フォーム設計表
- 移行／棚卸し用の列一覧 など

---

## 🧱 全体構成

### 1. Power Automate（クラウドフロー）

1. SharePoint REST API を叩いて、**サイトのリスト一覧を取得**するフロー  
   - 使用 URI（例）  
     ```text
     _api/web/Lists?
       $select=Title,Hidden,Description,Id,BaseType
       &$expand=RootFolder
       &$filter=(substringof('thread.skype_wiki',Title) eq false)
               and (IsCatalog eq false)
     ```
2. SharePoint REST API を叩いて、**リストの Hidden を更新**するフロー  
   - 指定した List GUID に対して `Hidden` プロパティを true / false で更新
3. SharePoint REST API を叩いて、**ターゲットリストの列（フィールド）情報を JSON で返す**フロー  
   - Power Apps 側から呼び出し、列情報を JSON 文字列として返却

> ※記事中では URL 変更（RootFolder の MoveTo）フローの例も紹介していますが、  
> 公開パッケージに含めるかどうかは運用方針に合わせて調整してください。

### 2. Power Apps（キャンバスアプリ）

- 入力欄で **SharePoint サイト URL** を指定
- 「リスト取得」ボタンからフローを呼び出し、JSON を受け取る
- `ParseJSON()` でコレクション展開し、ギャラリーに一覧表示
- Hidden 更新用のフローを呼び出して、リストの表示／非表示を切り替え
- 列情報の JSON を受け取り、タブ区切りのテキストに整形
- `Copy()` 関数でクリップボードへ送信 → Excel に貼り付け

---

## 📝 第三者ライセンス（Google Material Icons / Material Symbols）

本アプリでは **Google Material Symbols / Material Icons**（アイコンフォント／SVG 等）を利用しています。  
Material Icons / Material Symbols は **Apache License 2.0** で提供されています。

- 公式ドキュメント  
  - Material Icons: https://developers.google.com/fonts/docs/material_icons  
  - Material Symbols: https://developers.google.com/fonts/docs/material_symbols
- ライセンス本文（公式）  
  - https://github.com/google/material-design-icons/blob/master/LICENSE


> Icons by Google — Material Symbols / Material Icons (Apache License 2.0)
> 
> アイコン: Google Material Symbols / Material Icons（Apache License 2.0）

